---
import ContactForm from './ContactForm.astro';
import { getLangFromPath, languages, routes } from '../i18n/routes';
import { t } from '../i18n/ui';

const lang = getLangFromPath(Astro.url.pathname);
const { footer } = t(lang);

const currentYear = new Date().getFullYear();
const goodEWorkersEmail = 'hello@goodeworkers.org';
const emailLocalPart = 'hello';
const emailDomainPart = '@goodeworkers.org';
const githubUrl = 'https://github.com/GoodEWorkers';
---

<footer class="relative z-10 bg-white text-black font-inter">
  <div
    class="absolute -z-50 bg-transparent w-px h-px transform -translate-y-12"
    id="contact"
  >
  </div>
  <div class="max-w-screen-2xl mx-auto px-4 md:px-8 mb-4 pt-12 pb-10">
    <div class="flex flex-col">
      <h2 class="text-3xl sm:text-4xl font-clash font-bold">
        {footer.contactTitle}
      </h2>
      <div class="mt-6">
        <a
          href={`mailto:${goodEWorkersEmail}?subject=${encodeURIComponent(footer.mailSubject)}`}
          class="text-3xl min-[360px]:text-4xl sm:text-5xl font-bold leading-[1.1] hover:underline"
        >
          {emailLocalPart}<br />{emailDomainPart}
        </a>
      </div>
      <ContactForm />
    </div>
    <ul
      class="mt-24 md:mt-40 xl:mt-60 flex flex-col items-center text-sm space-y-2"
    >
      <li class="hover:underline">
        <a href={routes.legal[lang]} hreflang={languages[lang].htmlLang}>
          {footer.legalLink}
        </a>
      </li>
      <li>
        <!-- "Open source" is claimed repeatedly across the site; this is the
             first link that lets anyone verify it. -->
        {footer.openSource} <a
          class="hover:underline"
          href={githubUrl}
          rel="noopener">GitHub</a
        >
      </li>
      <li>
        {footer.poweredBy} <a
          class="hover:underline"
          href="https://netlify.com?utm_source=goodeworkers"
          rel="noopener">Netlify</a>
      </li>
      <li>{footer.copyright(currentYear)}</li>
    </ul>
  </div>
</footer>
